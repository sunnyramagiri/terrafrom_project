provider "aws" {
    region = "us-east-1" # need to select as required
    access_key =   "" # create the IAM user and download the file 
    secret_key = "" # create the IAM user and download the file
}